import 'package:flutter/material.dart';

class OpenSourceLibraries extends StatelessWidget {
  const OpenSourceLibraries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
